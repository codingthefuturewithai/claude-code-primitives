[project]
name = "ctfai-brand"
version = "1.0.0"
description = "CTFAI brand styling skill with PDF generation"
requires-python = ">=3.10"
dependencies = [
    "reportlab>=4.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
